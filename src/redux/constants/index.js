import * as aboutModalTypes from './aboutModalConstants';
import * as userTypes from './userConstants';
import * as threadTypes from './threadConstants';
import * as walkthroughTypes from './walkthroughConstants';

const reduxTypes = {
  aboutModalTypes,
  userTypes,
  threadTypes
};

export { reduxTypes as default, reduxTypes, aboutModalTypes, userTypes, threadTypes, walkthroughTypes };
